See AGENTS.md at the repo root — that is the authoritative project guide
(stack, architecture, hard rules, known gaps). This file exists only so
GitHub Copilot picks up the same instructions automatically.
